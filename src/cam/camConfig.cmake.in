# - Config file for the cam package
# It defines the following variables
#  cam_INCLUDE_DIRS - include directories for FooBar
#  cam_LIBRARIES    - libraries to link against
#  cam_EXECUTABLE   - the bar executable

set(cam_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${cam_DIR}/export_cam.cmake")

# These are IMPORTED targets created by camTargets.cmake


